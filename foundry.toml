[profile.default]
src     = "src"
test    = "test"
script  = "script"
out     = "out"
cache   = true
solc    = "0.8.20"
optimizer        = true
optimizer_runs   = 200
verbosity        = 2         # показує назви тестів; -vvvv покаже трейси

[profile.default.fuzz]
runs = 256                   # кількість fuzz-ітерацій

[rpc_endpoints]
sepolia = "https://rpc.sepolia.org"
